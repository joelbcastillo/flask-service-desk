{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "pytest",
            "type": "shell",
            "command": "pipenv run flask test"
        },
        {
            "label": "clean",
            "type": "shell",
            "command": "pipenv run flask clean"
        }
    ]
}
